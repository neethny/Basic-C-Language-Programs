#include<stdio.h>
#include<conio.h>
int main()
{
    float pi=3.14, r, area;
    scanf("%f",&r);
    printf("the radius of given cirlce %f \n", r);
    area=pi*r*r;
    printf("the area of the circle %f", area);
    getch();

}